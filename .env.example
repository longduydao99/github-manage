# Note: Scripts use gh CLI for auth (gh auth login). No SSH key config needed.

# --- Optional: script defaults (avoids passing --repo on every CLI call) ---
# GITHUB_ORG=your-org-or-username       # Owner/org name, e.g. zodinet
# GITHUB_REPO=your-repo-name            # Repo name only (no org prefix), e.g. my-repo
# Scripts combine these as ${GITHUB_ORG}/${GITHUB_REPO} when --repo is omitted.

# --- Optional: local LLM defaults for commit message generation ---
# LLM_PROVIDER=ollama
# LLM_MODEL=qwen2.5-coder:7b
